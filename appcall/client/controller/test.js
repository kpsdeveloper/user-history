/*var arr=[];
            e.preventDefault();
            var id=this._id;
            //Router.go('productDetail');
            //Session.set('idPro',id);
            var idarray=$('#id').val();
            //console.log(idarray);
            if(idarray){
               var arrID=idarray;
               arr.push(id,arrID);
               var aa=arr.toString()
               var arrayActive=aa.split(",");
               Session.set('arrayActive',arrayActive);//store to real working
               //console.log(arrayActive)
            }
            else{
                var arrID='';
                arr=[id];
            }

            console.log(arr);
             Session.set('id',arr);*/
             //store id to push